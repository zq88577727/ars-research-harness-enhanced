# Journal Profiles

Journal profiles declare submission constraints that should be checked at S9
before the project calls a manuscript "submission-ready." Keep the generic SCI
profile as the fallback, then add target-journal profiles as JSON files when a
real journal is selected.

Profiles should cover:

- article type and word limits
- abstract structure
- table/figure limits
- reference style
- required declarations
- reporting guideline policy

